/* ============================================================
   Nexusnote Dashboard — mock 数据（UI 原型阶段，已弃用）
   ------------------------------------------------------------
   所有卡片已切换为 Vault 真实数据；原 media 卡片改为由
   vaultReader.getRecentNotes() 驱动的「最近更新」列表。
   本文件保留以避免破坏潜在引用，可安全删除。
   ============================================================ */
