# Nexusnote 插件安装说明

Nexusnote 是一个 Obsidian 个人知识库仪表盘插件（Agent 操作中心）。
本文档介绍四种安装方式，按推荐程度从高到低排列。

---

## 方式一：社区插件市场（推荐，待上架）

上架 Obsidian 官方社区插件市场后，可直接在 Obsidian 内安装：

1. 打开 Obsidian 设置 → **社区插件** → 关闭「安全模式」。
2. 点击 **浏览**，搜索 `Nexusnote`。
3. 点击 **安装**，安装完成后点击 **启用**。

> 当前版本尚未提交至官方市场，请先用下方「方式二 / 方式三」安装。

---

## 方式二：BRAT 安装（Beta Reviewer's Auto-update Tool）

适合想第一时间获取更新的用户，支持一键更新。

1. 在 Obsidian 社区插件市场安装 **BRAT** 插件并启用。
2. 打开 BRAT 设置 → **Add Beta plugin**。
3. 在弹出的输入框中填写仓库地址：

   ```
   shuixiande/Nexusnote
   ```

4. 点击 **Add Plugin**，等待安装完成后启用 Nexusnote。
5. 日后更新：BRAT 设置 → **Update all beta plugins** 即可。

---

## 方式三：手动安装（从 GitHub Release 下载）

适合不想装 BRAT、希望固定版本的用户。

1. 打开仓库 Releases 页面：<https://github.com/shuixiande/Nexusnote/releases>
2. 下载最新版本中的三个核心文件：

   - `main.js`
   - `manifest.json`
   - `styles.css`

3. 在你的 Vault 中找到插件目录（若没有则新建）：

   ```
   VaultFolder/.obsidian/plugins/Nexusnote/
   ```

4. 将上面三个文件复制到该目录。
5. 打开 Obsidian 设置 → **社区插件**，关闭「安全模式」，在已安装列表中启用 **Nexusnote**。

> 提示：`.obsidian` 是隐藏目录。Windows 用户可在文件资源管理器地址栏输入路径；macOS 用户按 `Cmd+Shift+.` 显示隐藏文件。

---

## 方式四：从源码构建（开发者）

1. 克隆仓库：

   ```bash
   git clone https://github.com/shuixiande/Nexusnote.git
   cd Nexusnote
   ```

2. 安装依赖并构建：

   ```bash
   npm install
   npm run build
   ```

3. 构建产物 `main.js` 会生成在仓库根目录，将其与 `manifest.json`、`styles.css`
   一起复制到 Vault 的 `.obsidian/plugins/Nexusnote/` 即可（同方式三第 3–5 步）。

---

## 知识库目录结构约定

插件基于「四层知识库」结构工作（与根目录 `agent.md` 规则一致）。
首次使用时，在仪表盘点击 **一键部署知识库**，会自动创建以下目录：

| 层级 | 文件夹 | 用途 |
|------|--------|------|
| 1 | `1_原始资料（不可变原料）` | 导入的原始素材，保持不可变 |
| 2 | `2_创意想法（灵感燃料）` | 灵感与半成品 |
| 3 | `3_知识库（AI接管Wiki）` | 结构化知识 |
| 4 | `4_输出（成品出口）` | 最终成品 |

模板文件存放于 `templater/` 文件夹。

---

## 首次使用建议

1. 打开命令面板（`Ctrl/Cmd + P`），运行 **Nexusnote: 打开仪表盘**。
2. 点击 **一键部署知识库**，生成四层目录与 `agent.md` 规则文件。
3. 用 **新笔记 / 灵感 / 新素材** 按钮开始向知识库填充内容。
4. 用 **素材入库** 调用 Claudian 插件对原料层做智能处理。

---

## 常见问题

**Q：启用插件后提示「未通过安全模式」？**
A：设置 → 社区插件 → 关闭「安全模式」再启用即可。

**Q：仪表盘打不开？**
A：确认 Obsidian 版本 ≥ 1.8.0（manifest 中 `minAppVersion`）。低于该版本请升级 Obsidian。

**Q：更新后界面没变化？**
A：若用 BRAT，运行「Update all beta plugins」；若手动安装，重新下载三个文件覆盖原文件并重启 Obsidian。

**Q：可以删掉根目录的 `agent.md` 吗？**
A：可以，它只是部署时生成的规则说明文件，重新点击「一键部署知识库」会重新生成。

---

## 卸载

设置 → 社区插件 → 已安装 → Nexusnote → **禁用 / 卸载**。
插件不会删除你的 Vault 内容，仅移除仪表盘功能与 `templater/`、`agent.md` 等生成文件（手动删除这些文件不影响笔记）。
